# 生命力比對系統 — Claude Code 開發紀錄

> 專案路徑：`C:\Users\msi01\Desktop\AI\vitality_compare\`
> GitHub：`carolkao-fin/vitality-compare`
> 最後更新：2026-05-01

---

## 一、專案概覽

| 項目 | 內容 |
|------|------|
| 框架 | Streamlit |
| 主程式 | `app.py`（約 1757 行） |
| 相依套件 | `requirements.txt` |
| 持久化歷史 | `history_log.json`（自動生成，不進 git） |

### requirements.txt
```
streamlit>=1.31.0
pandas>=2.0.0
openpyxl>=3.1.0
numpy>=1.24.0
jieba>=0.42.1
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
xlsxwriter>=3.2.0
```

---

## 二、Git Commit 歷史（最新在上）

| Hash | 說明 |
|------|------|
| `cab0d07` | 歷史記錄跨工作階段持久化：JSON 檔儲存執行時間與結果，Excel 不保留 |
| `7e8faaf` | 歷史記錄全面升級：所有步驟記錄 + 執行時間 + 結果指標 + 刪除功能 |
| `9a23fd8` | 還原正確步驟順序（Step 3=比對分析, Step 4=文章比對），並加入前置條件備註 |
| `fc8bbed` | 修正步驟編號：文章比對改為 Step 3，比對分析改為 Step 4（後還原） |
| `90da8bd` | 側邊欄加入作業流程備註（A→E 步驟、品質檢核清單、欄位命名對照） |
| `00a5671` | 新增 Step 5 格式修正（統編補零 + 手機格式 + xlsxwriter 文字格式保護） |
| `70a3438` | 新增 Step 4 文章比對（關鍵字×組織名稱），補齊完整工作流程 |
| `6b91b8c` | Step 2 文字探勘加入兩階段進度條（爬取 0–50% / jieba 50–100%） |
| `dd3353b` | Step 3 比對分析加入進度條（9 個步驟即時顯示） |
| `8a7de55` | 調整 Step 3 介面：B 檔改名為最初期比對結果，移除 I 檔上傳 |
| `c30de89` | 整合三步驟工作流程：文章去重、文字探勘、比對分析 |
| `d7c6938` | 新增 I 檔上傳 + 修正生命力新聞欄位變體補值問題 |
| `7093e1f` | 修正三期分析邏輯三處問題 |
| `71ed036` | Initial commit: 生命力比對系統 Streamlit app |

---

## 三、App 標籤頁結構（最終版）

```
Tab 1: Step 1 文章去重
Tab 2: Step 2 文字探勘
Tab 3: Step 3 比對分析
Tab 4: Step 4 文章比對
Tab 5: Step 5 格式修正
Tab 6: 歷史記錄
```

---

## 四、各步驟說明

### Step 1 文章去重
- **函式**：`run_dedup(df)`
- **輸入**：上傳含 `article_title`、`url` 欄位的 Excel/CSV
- **輸出**：去重後資料、重複統計表
- **歷史記錄欄位**：原始筆數、去重後筆數、重複組合數

### Step 2 文字探勘
- **函式**：`run_text_mining(df, max_workers, timeout_sec, topk, use_textrank, progress_cb)`
- **輸入**：含 `url` 欄位的 Excel（通常接 Step 1 輸出）
- **核心技術**：
  - `requests` + `ThreadPoolExecutor` 並發爬取（最多 6 threads）
  - `BeautifulSoup` 解析 HTML（lxml parser）
  - `jieba.analyse.extract_tags`（TF-IDF）+ `textrank` 關鍵字提取
  - 規則式抽取：人名（`_extract_persons`）、組織（`_extract_orgs`）、計畫（`_extract_plans`）
- **進度條**：兩階段，爬取 0–50%、jieba 50–100%
- **輸出欄位**：`article_id`、`parsed_date`、`jieba_keywords`、`rule_keywords`、`keywords`、`fetch_error`、`content_chars`、`raw_preview`
- **歷史記錄欄位**：文章總數、未知日期、爬取失敗

### Step 3 比對分析
- **函式**：`run_analysis(df_A, df_B, progress_cb)`
- **輸入**：
  - A 檔：本期社創組織名單（含統一編號、組織名稱）
  - B 檔（最初期比對結果）：`data1140415比對生命力新聞_提供小編.xlsx`，**人工維護的小編清單**，非文字探勘輸出
- **分類邏輯**（三期稽核）：
  - `01 持續存在`：三期都有
  - `02 歷史補回`：之前有、本期重新出現
  - `03 本期真正新增`：首次出現
  - `08 疑似改名`
  - `09 疑似錯編`
- **進度條**：9 個步驟即時顯示
- **歷史記錄欄位**：總筆數、持續存在、歷史補回、本期真正新增、08 疑似改名、09 疑似錯編

### Step 4 文章比對
- **函式**：`run_article_match(df_mining, df_main, progress_cb)`
- **輸入**：
  - Step 2 輸出（文字探勘結果，含 `keywords` 欄）
  - Step 3 輸出（比對分析結果，含組織名稱）
- **前置條件**：Step 2 + Step 3 均須完成（介面有 📌 警告提示）
- **邏輯**：關鍵字 × 組織名稱交叉比對，利用 `_expand_name_variants()` 做模糊組織名匹配
- **歷史記錄欄位**：本期新增組織、僅歷史組織、文章命中篇數、文章總數

### Step 5 格式修正
- **函式**：`run_format_fix(uploaded_file)`、`make_excel_text_fmt(sheets)`
- **功能**：
  - 統一編號補零（7 位 → 8 位，使用 `norm_tax()`）
  - 手機號碼格式標準化
  - `xlsxwriter` 輸出時套用 `@` 文字格式，防止 Excel 自動吃掉前導零
- **歷史記錄欄位**：處理工作表數、修正欄位項目

---

## 五、側邊欄作業流程備註

```
🧭 作業流程（A → E）
A. 下載生命力新聞原始資料（含重複）
B. Step 1 文章去重
C. Step 2 文字探勘（爬網頁 + jieba）
D. Step 3 比對分析（搭配最初期比對結果）
E. Step 4 文章比對（關鍵字 × 組織名稱）

✅ 品質檢核清單
- 統編是否為 8 碼？
- 組織名稱是否含異體字（臺/台）？
- 爬取失敗率是否過高？

📋 欄位命名對照
- A 檔：本期社創組織名單
- B 檔：最初期比對結果（人工維護）
- Step 3 輸出：比對結果主檔
```

---

## 六、歷史記錄功能（最終版）

### 設計
| 區段 | 資料來源 | 下載按鈕 | 刪除按鈕 |
|------|----------|----------|----------|
| 本次工作階段 | `st.session_state.history`（含 Excel bytes） | ✅ | ✅ |
| 過去執行記錄 | `history_log.json`（無 Excel） | ❌ | ✅ |

### 持久化機制
```python
_HISTORY_FILE = os.path.join(os.path.dirname(os.path.abspath(__file__)), "history_log.json")
_HISTORY_MAX  = 200  # 最多保留 200 筆

def _load_history_log(): ...   # 讀取 JSON
def _save_history_log(records): ...  # 寫入 JSON（最多 200 筆）
def _append_to_log(rec): ...   # 過濾掉 excel bytes，防重複 ts 寫入
```

### 每步驟完成後呼叫模式
```python
_rec = { "step": "Step X ...", "ts": datetime.now().isoformat(timespec="seconds"),
         "input_files": [...], "results": {...}, "fname": "xxx.xlsx", "excel": bytes }
st.session_state.history.insert(0, _rec)
_append_to_log(_rec)
st.session_state.history_log = _load_history_log()
```

### session_state 初始化
```python
if "history" not in st.session_state:
    st.session_state.history = []
if "history_log" not in st.session_state:
    st.session_state.history_log = _load_history_log()
```

---

## 七、關鍵工具函式

| 函式 | 功能 |
|------|------|
| `norm_tax(x)` | 統一編號標準化（補零至 8 碼） |
| `norm_name(x)` | 組織名稱標準化（臺→台、去空白、小寫） |
| `norm_name_strong(x)` | 強版名稱標準化（另去除公司後綴） |
| `_expand_name_variants()` | 模糊組織名稱展開（含括號品牌名） |
| `clean_col(c)` | 欄位名稱清理（全形轉半形、去空白） |
| `make_excel(sheets)` | openpyxl 輸出 Excel |
| `make_excel_text_fmt(sheets)` | xlsxwriter 輸出 Excel（@文字格式保護） |
| `_render_rec(rec, show_download)` | 歷史記錄渲染 helper |

---

## 八、常見問題與決策紀錄

| 問題 | 決策 |
|------|------|
| B 檔是文字探勘輸出嗎？ | 否，是人工維護的小編清單（`data1140415比對生命力新聞_提供小編.xlsx`） |
| Step 3 和 Step 4 順序？ | Step 3 比對分析 → Step 4 文章比對（Step 4 依賴 Step 3 輸出） |
| 歷史 Excel 要跨 session 保留嗎？ | 否，只保留日期時間與結果數字 |
| history_log.json 重複寫入？ | 以 `ts` 欄位防重複（同一秒內多次 rerun 不會重複） |

---

## 九、待辦 / 可擴充方向

- [ ] Step 4 文章比對：提供命中明細下載
- [ ] Step 2 爬取失敗：自動重試機制（目前已有 requests Retry，但無手動重試按鈕）
- [ ] 歷史記錄：支援依步驟篩選
- [ ] 部署至 Streamlit Cloud（注意 `history_log.json` 在 Cloud 重啟後會消失）

---

## 十、修改紀錄（2026-05-02）

### 10-1 Step 2 文字探勘：修復「跳開」問題

**問題**：文字探勘完成後，點擊「下載」按鈕會觸發 Streamlit 整頁 rerun，
導致 `result_df`、`mining_bytes` 等區域變數消失，結果和下載按鈕跟著不見（使用者稱為「跳開」）。

**根本原因**：Streamlit 每次任何按鈕點擊都會重新執行整個 `app.py`，
結果變數只存在 `if st.button("🚀 開始文字探勘"):` block 內，rerun 時 block 不執行，變數消失。

**修改內容**：
- Session State 初始化新增 `st.session_state.mining_result = None`
- 探勘完成後將結果存入 `st.session_state.mining_result`（dict，含 `result_df`、`bytes`、`fname` 等）
- 結果顯示區塊（`st.success`、`st.dataframe`、`st.download_button`）移到 `if st.button(...)` 外，改從 session state 讀取
- 新增「清除結果」按鈕，讓使用者可主動重置

**關於增量儲存的討論**：
- 增量存入 session state 對本機有幫助（可中途下載部分結果）
- 但在 Streamlit Cloud 上，若伺服器被強制終止，session state 也會一併清空，增量儲存無法救回資料
- 結論：大量資料（> 200 篇 / 需超過 60 分鐘）仍建議在本機執行

**新增 UI 警告說明**：在 Step 2 頁面加入醒目的黃色警告框，說明：
- 兩階段耗時（爬取 3–5 分鐘、jieba 最長 90 分鐘以上）
- Streamlit Cloud 約 60 分鐘後強制終止，中途結果無法取回
- 本機執行指令：`streamlit run app.py`

---

### 10-2 Step 4 文章比對：生命力新聞欄位合併修正

**問題**：`run_article_match` 將 Step 4 新比對到的文章連結只寫入 `生命力新聞_new_match`，
從不回寫至 `生命力新聞`，導致兩欄永遠不同步。

**修改內容**（`run_article_match` 函式內，約 554 行）：
```python
# 將新連結合併回生命力新聞，使其成為完整累積版（歷史 + 本期新增，去重）
if true_new_pairs:
    df_main.at[i, "生命力新聞"] = _assemble_links(list(existing_pairs) + true_new_pairs)
```
- `生命力新聞` 現在 = 歷史連結 + 本期新增（自動去重、照年份排序）
- `生命力新聞_new_match` 仍保留，方便查看「本次新增了哪些」

---

### 10-3 社創平台網址 vs 生命力新聞 差異分析（`比對結果_更新後_20260501_182909.xlsx`）

**背景**：使用者發現兩欄數量不一致，詢問原因。

**分析結果**：
- 實際有連結的筆數兩欄相同，均為 **190 筆**
- 表面上「生命力新聞」非空值看似有 935 筆，是因為 745 筆填「無」（已查核確認無報導），而社創平台無報導時直接留空，**這是填寫慣例不同，不是程式問題**

**真正的差異（共 12 筆，全屬「持續存在」類）**：

| 分類 | 筆數 | 原因 |
|------|------|------|
| 社創平台有、生命力新聞無 | 4 | 新上架 `startup.sme.gov.tw` 組織，生命力新聞尚無對應報導 |
| 生命力新聞有、社創平台無 | 4 | 公司類型組織有報導，但未被社創平台收錄 |
| 兩欄都有但數量不同 | 4 | 社創平台 2025 年新增連結，生命力新聞未同步新增報導 |

**差異整理檔**：已輸出 `差異整理_社創平台vs生命力新聞.xlsx`（三個工作表分別對應三個分類）。

**結論**：這 12 筆均為資料本身的落差，非程式 bug，需人工判斷是否補填。
